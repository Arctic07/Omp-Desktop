# OMP Desktop 工程规则

本文件是本仓库所有 OMP 编程代理的强制工程约束。开始任何任务前必须完整读取本文件；任务结束前必须逐条自检。若本文件与系统指令、开发者指令或用户明确要求冲突，以上级别规则为准，并在交付中说明。

## 1. 任务开始前

1. 先阅读 `rules.md`、`package.json`，再阅读与任务直接相关的源码、配置和调用点。
2. 先确认现有目录、命名方式、组件模式和依赖版本，必须复用已有约定；禁止在不了解现有实现时新增第二套模式。
3. 修改导出的组件、函数、类型、路由或 Tauri command 前，必须检查所有引用方，并同步迁移调用方。
4. 明确任务范围。禁止顺手加入未要求的功能、重试、埋点、兼容别名、废弃路径或无关重构。
5. 发现用户已有改动时，视为有效工作内容；不得覆盖、回滚或清理无关改动。

## 2. 目录与分层

```text
src/
├─ components/   可复用、单一职责的界面组件
├─ layouts/      页面级布局与壳
├─ pages/        路由页面，只负责页面编排
├─ router/       路由定义和导航守卫
├─ stores/       跨页面共享状态
├─ composables/  可复用的 Vue 响应式逻辑
├─ utils/        与 Vue 无关的纯函数和工具
└─ styles/       设计令牌、全局样式和通用工具类

src-tauri/
├─ src/          Rust 应用入口与后端命令
├─ capabilities/ 最小权限声明
└─ tauri.conf.json
```

- 页面不得堆叠通用 UI、业务逻辑或重复样式；页面通过组件、composable、store 和 utility 组合能力。
- 组件只做一件清晰的事。复杂页面应拆成语义明确的子组件；单个组件持续超过约 200 行时必须评估拆分，禁止继续堆叠无关逻辑。
- `App.vue` 只负责应用根级装配；不得把侧栏、顶部栏、页面内容或业务状态全部写入其中。
- `components/` 中的组件必须通过 typed props / emits 暴露契约；不得依赖父组件内部变量名或隐式 DOM 结构。
- 跨两个页面复用的 UI、逻辑或样式必须提取；第三次出现相似实现前必须先抽象。不要为了假设中的复用提前创建空壳抽象。

## 3. Vue 3 与 TypeScript

- 使用 Vue 3 Composition API 和 `<script setup lang="ts">`。
- Props、emits、store 状态、事件参数和函数边界必须有明确类型；禁止新增 `any`、`@ts-ignore`、`@ts-expect-error`，确有必要时必须写明原因并限制范围。
- 优先使用 `computed`、`watch`、`ref` 和 composable 管理响应式逻辑；禁止在模板中写复杂表达式或副作用。
- 组件事件名称、路由名称、store 字段和 CSS class 使用稳定、语义化、可搜索的命名；避免 `data`、`temp`、`handleThing` 等无意义名称。
- import 顺序固定为：外部依赖、空行、本地模块；同一组内保持稳定顺序。删除无用 import、变量和注释。
- 不保留空函数、伪实现、无效 fallback、`TODO: implement`、永远不可达分支或仅为通过类型检查而存在的代码。

## 4. Arco Design 与样式

- 优先使用 Arco Design Vue 组件和现有设计令牌；只有现有组件无法表达需求时才新增基础样式。
- 通用颜色、间距、圆角、阴影、字号必须使用 CSS 变量或已有 token；禁止在多个文件复制魔法数值。
- 局部样式优先放在组件 `<style scoped>`；跨组件复用的样式放入 `src/styles/`，并使用语义化工具类或组件类。
- 禁止用内联 style 固化静态样式；动态值必须使用 CSS 变量、绑定 class 或经过说明的动态 style。
- CSS class 使用组件/区域前缀，避免 `.title`、`.container` 等全局易冲突名称；禁止无理由使用深层选择器覆盖 Arco 内部实现。
- 新增重复的布局、空状态、按钮组、表单项或状态标签前，必须先查找并复用现有组件或抽取通用组件。
- 保持桌面窗口的全高布局、键盘可达性、清晰焦点态和足够的颜色对比度；交互元素必须有可理解的 aria label 或可见文本。

## 5. 路由、状态与 Tauri

- 路由页面只处理路由参数和页面编排；共享状态放入已有 store，局部状态留在组件内，不得为单个值创建全局 store。
- 不在多个组件散落相同的路由字符串；使用已有路由名称、常量或集中映射。
- Tauri `invoke`、事件监听和窗口 API 必须通过集中、可测试的前端 API 层封装；页面组件不得到处直接拼接 command 名称。
- 新增 Rust command 时同步更新前端类型、调用方和 `capabilities`，只授予最小权限；禁止为了方便启用 `shell`、`fs` 或通配权限。
- 不把密钥、令牌、机器路径或用户隐私写入源码、日志、localStorage 或提交内容。

## 6. 代码格式与完成检查

- 文件统一 UTF-8、LF 换行、末尾保留换行；缩进 2 个空格；禁止行尾空格和无意义空行。
- 延续当前项目格式：TypeScript/Vue 使用单引号、无分号、尾随逗号遵循现有代码；不要在同一文件混用风格。
- 优先使用仓库已有 formatter / lint / format:check 脚本。若项目尚未配置格式化工具，不得声称已完成 formatter 检查；必须逐个检查改动文件的缩进、换行、引号、import、模板和 CSS 层级，并在交付中明确说明。
- 修改完成后至少执行与改动相关的格式检查；涉及 TypeScript/Vue 时执行 `pnpm typecheck`，涉及构建配置或 Tauri 时执行对应的构建/启动检查。若用户或上级规则明确禁止运行，遵守禁止项并报告未运行原因。
- 发现格式检查、类型检查、构建或运行错误后，必须修复根因并重新检查；不得通过关闭检查、忽略错误、放宽类型或隐藏输出来“通过”。
- 交付前检查：无调试输出、无临时文件、无无关改动、无未使用依赖、无未完成占位实现；确认新增文件已纳入正确目录和忽略规则。

## 7. 交付与版本控制

- 交付说明必须列出实际修改文件、行为变化、已执行检查、未执行检查及剩余风险；不得捏造未运行的命令结果。
- 默认不执行 `git add`、commit、push 或删除用户文件；只有用户明确要求时才执行。
- 不创建与当前任务无关的文档、示例、测试数据或抽象层。
- 先交付可工作的完整切片，再说明确实无法完成的外部前置条件；禁止提交半成品、空壳、假数据或“后续再实现”的核心路径。
