# OMP Desktop 工程规范

本文件是 OMP Desktop 的强制工程规范，供开发者和 AI 开发代理共同遵守。

## 1. 技术栈与总原则

- 前端固定使用 Vue 3、TypeScript、Composition API 和 `<script setup lang="ts">`。
- 桌面应用固定使用 Tauri 2；本地系统、文件、Git、进程和高性能核心能力优先使用 Rust 实现。
- 当前阶段不接入 Agent SDK。未获得明确需求前，不得新增 SDK、SDK 适配层、空壳接口、Mock、占位实现或 `TODO: implement`。
- Vue/TypeScript 负责界面、交互和展示；Rust 负责安全边界、系统能力、长任务、并发、数据处理和性能关键路径。
- 代码必须精炼、稳定、可读、可维护。禁止垃圾代码、重复代码、无效抽象、临时补丁和为了通过检查而存在的代码。
- 需求实现前必须先理解需求、划分边界、设计数据流、评估错误场景和依赖，再开始编码。
- 本规范中的“必须”“禁止”是硬性要求。若与系统指令、开发者指令或用户最新明确要求冲突，以更高优先级规则为准。

## 2. 开发前架构检查

每项非 trivial 改动开始前，必须完成以下检查；简单单点修复也必须至少确认现有调用方式和影响范围：

1. 明确需求范围、输入输出、成功条件、失败行为和不属于本次需求的内容。
2. 阅读 `rules.md`、`package.json`、相关配置、目标源码和所有直接调用点；优先复用现有模式。
3. 确定功能属于界面层、前端逻辑层、IPC 层、Rust command 层还是 Rust service 层。
4. 设计模块边界、数据流、状态所有权、并发模型、取消方式、权限边界和错误传播方式。
5. 检查已有工具、组件、composable、store、API 封装和依赖；已有能力可以复用时不得重新实现。
6. 如需新依赖，先确认现有依赖无法满足，再评估版本、API 稳定性、维护状态、许可证、跨平台支持、包体积、安全风险和性能影响。
7. 先定义前后端 DTO、command 名称、事件协议和错误契约，再实现具体代码。
8. 记录可能的边界问题：空输入、非法路径、权限不足、文件不存在、超大数据、进程失败、并发请求、取消和重复操作。

禁止因为“以后可能用到”提前创建空目录、空接口、无调用的抽象层或未完成的扩展点。

## 3. 分层和目录结构

### 3.1 前端目录

```text
src/
├─ components/       可复用、单一职责的界面组件
├─ layouts/          页面级布局和应用壳
├─ pages/            路由页面，只负责页面编排
├─ router/           路由定义和导航守卫
├─ stores/           跨页面共享状态
├─ composables/      可复用的 Vue 响应式逻辑
├─ utils/            与 Vue 无关的纯函数和集中式 API 封装
└─ styles/           设计令牌、全局样式和通用样式
```

### 3.2 Rust 目录

```text
src-tauri/
├─ src/
│  ├─ lib.rs         Tauri 初始化、插件初始化和 command 注册
│  ├─ commands/      薄 command 层，负责输入校验和调用 service
│  ├─ services/      按领域组织的核心业务实现
│  ├─ models/        前后端共享语义的请求、响应和事件模型
│  ├─ errors.rs      统一错误类型和错误码
│  ├─ state.rs       应用级状态、任务状态和资源生命周期
│  └─ utils/         Rust 内部可复用的纯工具
├─ capabilities/     最小权限声明
└─ tauri.conf.json
```

目录只在确实需要时创建，不得为了形式创建空壳模块。`lib.rs` 不得堆叠领域业务逻辑；随着功能增长，必须按 workspace、Git、process、session 等领域拆分。

### 3.3 单文件行数硬限制

- 任何新建或修改后的单个文件不得超过 1000 行，空行和注释也计入行数。
- 推荐单文件控制在 300 至 600 行以内；接近 800 行时必须优先拆分，再继续添加功能。
- 组件持续超过约 200 行，必须评估是否拆成语义明确的子组件、composable 或 utility。
- 存量超限文件不得继续无上限增长；修改其业务逻辑前，必须先拆分到 1000 行以内，除非用户明确批准暂缓。
- 拆分必须保持单一职责、清晰依赖方向和稳定对外契约，禁止仅机械切文件或制造转发层来规避行数限制。

## 4. 前端 Vue 和 TypeScript 规范

- 使用 Vue 3 Composition API 和 `<script setup lang="ts">`。
- Props、emits、store 状态、事件参数、函数参数、返回值和 API 边界必须有明确类型。
- 禁止新增 `any`、`@ts-ignore`、`@ts-expect-error`。确有外部类型缺失时，必须限制范围并写明可验证的原因。
- 页面只处理路由参数和页面编排；通用 UI 放入 `components/`，共享响应式逻辑放入 `composables/`，跨页面状态放入已有 store，纯函数放入 `utils/`。
- `App.vue` 只负责应用根级装配，不得堆叠侧栏、顶部栏、页面内容和业务状态。
- 组件只做一件清晰的事；必须通过 typed props 和 emits 暴露契约，不得依赖父组件变量名、隐式 DOM 结构或未声明的全局状态。
- 模板中不得编写复杂表达式、数据处理循环或副作用；复杂逻辑放入 `computed`、函数或 composable。
- 输入、选择、展开、拖拽等高频临时 UI 状态留在前端；不得为单个值创建全局 store。
- 同一状态只保留一个事实来源；禁止在组件、store 和后端重复维护没有同步契约的副本。
- 异步请求必须处理 loading、成功、失败、取消和过期响应；旧请求结果不得覆盖新请求状态。
- import 顺序固定为：外部依赖、空行、本地模块；延续当前项目的单引号、无分号和尾随逗号风格。
- 删除未使用的 import、变量、类型和注释；禁止保留死代码、空函数、永远不可达分支和无效 fallback。
- 大列表优先复用现有虚拟列表依赖或组件；不得为已有能力重新造轮子。

### 4.1 UI 和样式

- 优先复用项目已有组件、图标、设计令牌和样式工具；不得无理由引入第二套 UI 体系。
- 通用颜色、间距、圆角、阴影和字号必须使用现有 CSS 变量或设计 token，禁止多处复制魔法数值。
- 局部样式优先使用 `<style scoped>`；跨组件样式放入 `src/styles/`。
- 禁止用内联 style 固化静态样式；动态样式使用 CSS 变量、绑定 class 或有明确理由的动态 style。
- CSS class 使用组件或区域前缀，避免 `.title`、`.container` 等全局易冲突名称。
- 桌面窗口必须保持全高布局、键盘可达性、清晰焦点态和足够颜色对比度；交互元素必须有可理解的 aria label 或可见文本。

## 5. Rust 核心代码规范

### 5.1 Rust 的职责

以下能力默认优先放在 Rust：

- 文件系统、工作区扫描、路径安全、文件监听和大文件处理。
- Git 状态、Diff 解析、Stage、Unstage、Commit、Pull 等仓库操作。
- 全文搜索、索引、二进制识别、Diff 和结构化数据解析。
- 子进程启动、参数构造、输出读取、取消、超时、退出状态和资源回收。
- 并发任务调度、持久化、缓存和其他性能关键路径。
- 需要隐藏密钥或接触操作系统安全存储的能力。

Vue/TypeScript 默认只负责调用这些能力并展示结果，不得在前端复制一套文件、Git 或进程核心实现。

### 5.2 Rust 实现要求

- `lib.rs` 只保留应用初始化、插件初始化和 command 注册；command 必须薄，领域逻辑必须放入 service。
- Rust 内部使用 `snake_case`；跨 IPC 的字段必须显式定义序列化名称，TS 类型和 Rust DTO 必须同步。
- 所有外部输入必须在 Rust 边界校验。路径必须规范化并确认位于工作区内；Git 路径、命令参数和文件大小必须有明确限制。
- 失败操作必须返回结构化 `Result`；运行时不得用 `unwrap`、`expect` 或 panic 处理用户输入、文件、网络和进程错误。
- 避免不必要的 `clone`、分配、复制和重复解析；优先借用切片、流式处理、边界明确的集合和有上限的缓存。
- 文件、搜索、Git 和进程操作必须有内存、输出大小和执行时间边界，禁止无上限读取或收集输出。
- 阻塞式文件、Git、进程或 CPU 密集工作不得阻塞异步运行时；使用合适的 `spawn_blocking`、专用线程或任务队列。
- 长任务必须支持状态查询或事件通知、取消和失败回收；任务结束后必须释放进程、句柄、临时文件和监听器。
- 跨平台代码必须明确处理 Windows、macOS 和 Linux 差异；禁止依赖只在开发机可用的路径、shell 语法或环境变量。
- 重要文件写入应采用临时文件加原子替换等方式，避免应用中断导致配置或数据损坏。
- 不得为了性能盲目复杂化；先确认真实瓶颈，再进行可验证的优化，并保留简单、稳定的实现。

## 6. Tauri IPC 和权限规范

- 所有 `invoke`、事件监听和窗口 API 必须通过集中式前端 API 层封装；页面和普通组件不得直接拼接 command 名称。
- 一个 command 只表达一个清晰的业务操作；命名稳定、参数明确、返回值可序列化。
- 新增或修改 command 时，必须同步更新 Rust DTO、前端类型、`desktopApi` 调用方和 `capabilities`。
- 高频交互不得逐次穿过 IPC；应在前端聚合，或由 Rust 提供批量、分页、流式和增量接口。
- 大数据不得无界地通过 IPC 传输；使用分页、分块、摘要、临时文件或受限响应。
- 长任务使用任务 ID 和类型化事件；至少区分开始、进度/输出、完成、失败和取消，事件必须能识别所属任务。
- IPC 错误必须包含稳定错误码和用户可用信息；不得把堆栈、密钥、令牌或不必要的绝对路径直接返回前端。
- Tauri capabilities 只授予最小权限。禁止为了方便启用通配的 shell、fs 或系统权限。
- 需要执行外部程序时，使用固定程序、参数数组和受控工作目录；禁止通过字符串拼接执行任意 shell 命令。

## 7. 依赖管理规范

- 添加依赖前必须先搜索并确认项目现有依赖不能满足需求；能复用现有能力时禁止重复引入。
- 每个新依赖必须有明确用途，并评估 API 稳定性、维护活跃度、版本兼容性、许可证、跨平台支持、包体积、编译时间和安全风险。
- 依赖必须锁定可复现版本，默认关闭不需要的 feature；不得引入功能重叠、无人维护或仅为几行工具函数服务的依赖。
- 必须优先使用成熟、文档完整、可在目标平台稳定运行的依赖，不得只根据网上示例或暂存 API 编码。
- 前后端分别遵守 `pnpm-lock.yaml` 和 `Cargo.lock` 的可复现要求；修改依赖后必须检查锁文件和构建链影响。
- Agent SDK 在当前阶段属于明确禁止新增的依赖，除非用户后续明确要求接入并重新完成架构评估。

## 8. 状态、并发和可靠性

- 先定义状态所有权：UI 瞬时状态归 Vue，共享业务状态归明确的 store 或 Rust state，持久化数据归明确的数据层。
- 所有异步流程必须考虑重复触发、竞态、过期结果、取消、超时、进程退出和应用关闭。
- 默认不添加隐式重试、静默降级或吞错逻辑；只有需求明确时才实现，并定义次数、间隔、终止条件和用户反馈。
- Git、文件和进程失败必须可恢复或给出明确错误；Git 不可用时只能按已定义的降级行为处理，不得伪造成功。
- 日志必须有明确目的、合适级别和可定位上下文；禁止输出密钥、令牌、完整隐私内容和无意义调试日志。
- 缓存必须有容量、生命周期和失效策略；禁止无界缓存、全量复制和依赖隐式全局变量。
- 任何并发共享资源必须明确同步策略和生命周期；禁止使用竞态不明确的可变全局状态。

## 9. 安全规范

- 不信任前端传入的 root、path、命令、参数、URL、文件内容和用户文本；Rust 必须重新校验。
- 防止路径穿越、符号链接逃逸、任意命令执行、参数注入、未授权文件访问和资源耗尽。
- 密钥、令牌和用户隐私不得写入源码、日志、localStorage、提交内容或普通配置文件；优先使用操作系统安全存储。
- 展示不可信文本时不得无理由使用 `v-html` 或其他原始 HTML 注入方式；确需使用时必须有明确的清洗边界。
- capability、文件访问、进程执行和网络访问遵循最小权限原则；新增权限必须说明用途和范围。

## 10. 验证与质量门禁

实现完成后必须进行与改动相关的验证，不得只凭代码看起来正确就交付：

- TypeScript/Vue 改动：执行 `pnpm typecheck`，并进行相关页面或交互的实际检查。
- Rust 改动：执行格式检查、`cargo check` 或对应的目标构建，并验证受影响的 command 或场景。
- Tauri 配置、权限或 IPC 改动：检查 command 注册、前端类型、调用方、capabilities 和实际启动路径。
- Bug 修复：先复现问题，再确认修复后原场景不再触发；可长期保留的行为回归才添加测试。
- 测试应验证消费者可观察的行为、边界、错误、状态转换和并发不变量，禁止测试实现细节、源码文本或无意义的 `not.toThrow`。
- 不得通过关闭检查、放宽类型、吞掉错误、隐藏输出或增加无效 fallback 来“通过”验证。
- 不得声称执行过未执行的命令、测试、构建、浏览器检查或运行验证；未执行项和剩余风险必须明确说明。

## 11. 变更和交付规范

- 只修改实现需求所需的文件；禁止夹带无关重构、无关格式化、无关依赖和无关行为变化。
- 修改导出的组件、函数、类型、路由、store 或 Tauri command 前，必须检查全部引用方并同步迁移。
- 删除或替换旧实现时必须完成干净切换；不得保留无调用的别名、兼容壳、废弃路径或重复实现。
- 不得覆盖、回滚或清理用户已有且与当前需求无关的改动。
- 交付说明必须列出实际修改文件、行为变化、已执行检查、未执行检查和剩余风险。
- 默认不得执行 `git add`、commit、push 或删除用户文件，除非用户明确要求。
- 交付前必须确认：无调试输出、无临时文件、无空壳实现、无未使用依赖、无未完成占位、文件行数符合上限、格式和命名统一。

## 12. AI 执行顺序

AI 实现任何功能时必须按以下顺序执行：

1. 阅读本规范、项目配置、相关源码和调用点。
2. 给出简短的需求拆解、架构边界、数据契约、依赖判断和风险清单。
3. 复用已有组件、工具、依赖和 API 封装；确认没有重复造轮子。
4. 先实现稳定的最小完整链路，再补齐错误、取消、边界和资源清理。
5. 进行针对性格式、类型、构建和行为验证，并修复根因。
6. 静态复审文件行数、职责边界、权限、性能、调用方和遗留代码。
7. 按第 11 节交付，明确事实、证据、未执行项和剩余风险。

任何阶段都不得交付半成品、假数据、空函数、无效 fallback、隐藏错误或“后续再实现”的核心路径。
