# Omp Desktop 接入 omp RPC、本地存储与跨版本数据方案

> 状态：待评审（自检章节见 §10）
> 范围：把 `omp`（oh-my-pi）以 RPC 模式打包进 Omp Desktop（Tauri 2 + Vue 3），并设计本地数据库、数据目录与跨版本迁移机制，保证「发新版 → 老用户升级 → 数据不坏」。
> 依据：本仓库 `rules.md`（强制工程规范）、omp 官方文档与已发布产物（§2 逐条给出出处）。

---

## 1. 结论摘要

架构一句话：**Rust 宿主 + omp 子进程（sidecar）走 stdio NDJSON-RPC；Rust 独占进程、协议、落盘与迁移；Vue 只做投影。**

三个关键决定：

| 决定 | 内容 | 理由 |
| --- | --- | --- |
| D1 引擎进程化，不内嵌 | 把官方预编译 `omp` 二进制作为 Tauri sidecar 拉起，`--mode rpc` | omp 仅有 Bun（`bun>=1.3.14`）运行形态或官方预编译二进制；内嵌源码等于把 47MB/2676 文件的 TS 运行时纳入我们的构建链，且其会话/配置格式随版本演进，无法由我们冻结 |
| D2 数据分三层，所有权互斥 | `app.sqlite`（我们的） / `omp-agent/`（omp 的，经 `PI_CODING_AGENT_DIR` 收进我们数据根） / `runtime/<version>/`（可整体替换） | 「新版整理旧版数据」只能发生在我们拥有的数据上；omp 自己的 `*.jsonl` 由 omp 按 header `version` 自行迁移，我们永不改写 —— 这是升级不坏数据的根本保证 |
| D3 迁移与整理分离 | 结构迁移（事务 + 备份 + 单向门闩）与「整理」（幂等、可中断、可重入的 maintenance pass）是两条独立流程 | 迁移失败必须是原子回滚；整理失败只应降级不影响可用性。混在一起会让「一次失败的整理」变成「应用起不来」 |

---

## 2. 事实基线（设计的证据基础）

### 2.1 RPC 协议

| 事实 | 出处 |
| --- | --- |
| RPC 模式 = stdin/stdout 上的 NDJSON（每行一个 JSON 对象），启动命令 `omp --mode rpc [常规 CLI 选项]` | <https://raw.githubusercontent.com/can1357/oh-my-pi/main/docs/rpc.md> |
| 启动后先发 ready 帧，广告协议版本与帧上限：`{"type":"ready","protocolVersion":1,"supportedProtocolVersions":[1,2],"maxFrameBytes":1048576,"maxReassembledFrameBytes":67108864}` | 同上 |
| 协议 v1 会把 stdout 单帧硬限在 1 MiB，超限走「有界 fallback」（会丢内容）；v2 通过 `negotiate_protocol` 启用 `rpc_chunk` 分片（base64 分段 + `chunkId/index/count/byteLength` 校验），可无损重组到 64 MiB | 同上 |
| `prompt` / `abort_and_prompt` 是**立即 ack**：ack ≠ 本轮结束 | 同上 |
| **轮次完成判定**：只有 `agent_end` 且 `isTerminal !== false` 才算运行结束（`isTerminal:false` 表示维护/异步投递又排了活，会话会继续）；纯本地命令靠 `data.agentInvoked:false` 或后续 `prompt_result` | 同上 |
| 流式期间再发 `prompt` 必须带 `streamingBehavior: "steer" \| "followUp"`，否则失败 | 同上 |
| `get_messages_page { cursor?, limit? }` 提供稳定分页：返回 `messages`/`totalMessages`/不透明 `nextCursor`（cursor 绑定会话 id、当前 leaf 与消息数）；流式或压缩期间拒绝分页；失败带机器可读 `code`：`session_busy` \| `stale_cursor`；单页上限 256 条 | 同上 |
| `bash` 命令**并发派发**：长命令执行期间服务器仍读后续命令；跨并发命令的响应顺序不保证，客户端**必须按 `id` 匹配**而非到达顺序 | 同上 |
| `login` 只有在 provider 给出授权 URL 之后才转发普通 OAuth 提示；标记 `secret: true` 的输入提示一律拒绝并回失败（引导用户去终端 UI），RPC 不协商密文输入 | 同上 |
| `security://` 为 omp 保留 scheme，宿主 **不能**注册或遮蔽 | 同上 |
| 自动会话标题在 RPC 模式默认关闭，`setTitle` UI 事件默认抑制（`PI_RPC_EMIT_TITLE=1` 可单独打开该事件） | 同上 |
| 事件面（节选）：`agent_start/end`、`turn_start/end`、`message_start/update/end`、`tool_execution_start/update/end`、`auto_compaction_*`、`auto_retry_*`、`retry_fallback_applied/succeeded`、`model_changed`、`thinking_level_changed`、`ttsr_triggered`、`todo_reminder/todo_auto_clear`、`irc_message`、`notice`、`goal_updated` | 同上 |
| 出站帧类别：ready、response、AgentSessionEvent、`extension_ui_request`、`host_tool_call/cancel`、`host_uri_request/cancel`、`extension_error`、`available_commands_update`、`prompt_result`、subagent 帧、slash 命令侧信道 | 同上 |
| 命令面：prompt/steer/follow_up/abort/abort_and_prompt/new_session、negotiate_protocol、get_state、set_fast_mode、get_available_commands、get_messages、**get_messages_page**、switch_session、branch、get_branch_messages、export_html、handoff、compact、set_model/cycle_model/get_available_models、set_thinking_level/cycle_thinking_level、set_steering_mode/set_follow_up_mode/set_interrupt_mode、set_auto_compaction、set_auto_retry/abort_retry、bash/abort_bash、get_session_stats、get_last_assistant_text、set_session_name、get_login_providers、login、set_todos、set_host_tools、set_host_uri_schemes、set_subagent_subscription、get_subagents、get_subagent_messages | 同上 |
| stdin 关闭后：拒绝悬挂的扩展 UI/host-tool/host-URI 请求，排空已接受命令，dispose 会话，投递剩余 stdout，退出码 0；客户端**必须持续读 stdout**，否则进程迟迟不退出 | 同上 |
| 未识别命令的 response 不带 `id`（即便请求带了 `id`）；解析/派发异常回 `command:"parse"` 且 `id` 为 undefined | 同上 |

### 2.2 CLI 与运行时开关

| 事实 | 出处 |
| --- | --- |
| `--mode` 取值含 `rpc`；`--cwd`、`--config`、`--add-dir`、`--provider/--model/--smol/--slow/--plan`、`--thinking`、`--approval-mode {always-ask\|write\|yolo}`、`--no-session`、`--session-dir`、`--resume/-r/--session`、`--fork`、`--no-tools/--no-lsp/--no-pty/--no-extensions/--no-skills/--no-rules`、`--trusted-extension/-e/--hook/--plugin-dir` 等均为字符串/可选值开关 | `packages/coding-agent/src/cli/flag-tables.ts`（npm 包内源码，v18.2.6） |
| `sessionDir` 默认取环境变量 `PI_CODING_AGENT_SESSION_DIR` | `packages/coding-agent/src/cli/args.ts` 同版本 |

### 2.3 磁盘布局与路径开关

| 事实 | 出处 |
| --- | --- |
| 配置根 = `path.join(os.homedir(), PI_CONFIG_DIR ?? ".omp")`，即 `PI_CONFIG_DIR` 是**home 下的目录名**，不是绝对路径；`install-id` 存在配置根（默认 `~/.omp/install-id`，0o600） | `packages/utils/src/dirs.ts`（`@oh-my-pi/pi-utils` v18.2.6） |
| `PI_CODING_AGENT_DIR` 重定位 `~/.omp/agent`：全局 `config.yml`、认证库 `agent.db`、会话、blobs 等全部随之移动 | <https://raw.githubusercontent.com/can1357/oh-my-pi/main/docs/settings.md> |
| 会话文件：`<agentDir>/sessions/<encoded-cwd>/<timestamp>_<sessionId>.jsonl`；`<encoded-cwd>` 由规范化的 cwd 派生（home 下 `-<relative>`，temp 下 `-tmp-<relative>`，其余 `--<encoded-absolute>--`，分隔符替换为 `-`） | <https://raw.githubusercontent.com/can1357/oh-my-pi/main/docs/session.md> |
| 会话格式：JSONL，物理开头是 256 字节定宽 title 槽，随后是 header（`{"type":"session","version":3,"id":…,"cwd":…}`）与 append-only 的 `SessionEntry`；entry 用 `id`/`parentId` 构成树，`leafId` 可移动 | 同上 |
| 会话版本迁移 v1→v2→v3 由 omp 在 `setSessionFile` 加载时执行；迁移后「标记为需要整文件重写」，在下一次持久化时重写 | 同上 |
| 无法解析/非法的会话文件按「空会话」处理并被新会话替换（`[]` → new initialized session at that exact path） | 同上 |
| blob 外置：图片等落在 `<agentDir>/blobs/<sha256>`，引用写作 `blob:sha256:<hash>`；>500,000 字符字符串会被截断 | 同上 |
| 提示历史是独立 SQLite：`<agentDir>/history.db`（表 `history` + FTS5） | 同上 |
| 设置：`<agentDir>/config.yml`（兼容读 `config.yaml`）；旧 `settings.json` 只在两个 YAML 都不存在时迁移一次并改名 `.bak`；项目级 `<cwd>/.omp/config.yml` 读取，但设置命令不写任意项目键 | <https://raw.githubusercontent.com/can1357/oh-my-pi/main/docs/settings.md> |
| 维护命令：`omp gc` 预览、`omp gc --apply` 执行 —— 清扫无引用 blob、归档冷会话、checkpoint 数据库 WAL；压缩 `.jsonl.gz`；与「模型上下文压缩」是两件事 | docs/session.md |
| 作者身份：`install-id` 为随机 UUID，与主机名/用户名/硬件/账号无关，路径为配置根下的 `install-id`，与 profile 无关 | <https://raw.githubusercontent.com/can1357/oh-my-pi/main/docs/install-id.md> |

### 2.4 发布形态

| 事实 | 出处 |
| --- | --- |
| npm 包 `@oh-my-pi/pi-coding-agent@18.2.6`：bin `omp → dist/cli.js`，`engines.bun >= 1.3.14`，解包 47.3 MB / 2676 文件 | <https://registry.npmjs.org/@oh-my-pi/pi-coding-agent/latest> |
| GitHub Release 提供**各平台预编译二进制**：`omp-windows-x64.exe`、`omp-windows-arm64.exe`、`omp-darwin-arm64`、`omp-darwin-x64`、`omp-linux-x64`、`omp-linux-arm64`、`omp-linux-musl-{x64,arm64}`；体积 187–208 MB | <https://api.github.com/repos/can1357/oh-my-pi/releases/latest> |
| 官方 Windows 安装器默认装到 `%LOCALAPPDATA%\omp\omp.exe`，也支持 `-Source`（bun 全局安装）与 `-Binary` | `scripts/install.ps1`（仓库 main 分支） |
| 发行产物带 SLSA provenance 证明与签名 | npm registry `dist.attestations` |

### 2.5 本仓库现状（接入要落到的地基）

| 事实 | 位置 |
| --- | --- |
| 前端栈 Vue 3 + TS + `<script setup>`；Rust 负责系统能力；**当前阶段禁止接入 Agent SDK**，用户本次已明确要求接入（属「用户最新明确要求」，可覆盖该禁令本身，但不豁免其余规范） | `rules.md` §1、§7 |
| 已有 command：附件导入/粘贴（`import_composer_files`、`save_composer_paste`）、工作区（列目录/读写/搜索/定位）、Git（review/stage/unstage/commit/pull）、模型目录探测 | `src-tauri/src/commands/`、`src/lib.rs` |
| 前端 IPC 全量经集中封装 `src/utils/desktopApi.ts`，页面不拼 command 名 | `src/utils/desktopApi.ts`、`rules.md` §6 |
| 附件当前落在 `%TEMP%/omp-desktop/scratch/<sessionId>/pasted`（临时目录，重启即失效语义） | `src-tauri/src/services/attachments.rs:47-54` |
| 应用设置当前存 `localStorage['omp-desktop.settings']`（无版本号、无迁移） | `src/utils/appSettingsStorage.ts` |
| 会话/对话条目目前是前端内存态 mock（`ConversationFeedEntry` 等），无持久化 | `src/utils/conversationTypes.ts`、`src/composables/useAppShell.ts` |
| Rust 目前无 `state.rs`、无 DB、无子进程托管；`Cargo.toml` 依赖仅 base64/reqwest/serde/serde_json/tauri/tauri-plugin-dialog/windows-sys | `src-tauri/Cargo.toml` |
| capabilities 最小化（`core:default` + `dialog:default`） | `src-tauri/capabilities/default.json` |

---

## 3. 总体架构

```
┌──────────────────────────────────────────────────────────────────────┐
│ Renderer (Vue 3)                                                     │
│  - 会话流 / 权限卡 / 设置 / 工作面板（既有组件继续用）                  │
│  - 只经 utils/agentApi.ts 调用；不拼 command 名、不直连子进程          │
└───────────────▲──────────────────────────────┬───────────────────────┘
        Tauri events（每会话一条 channel）      │ invoke（typed DTO）
┌───────────────┴──────────────────────────────▼───────────────────────┐
│ Rust（Tauri 宿主 = 唯一权威）                                         │
│  runtime/      进程池：spawn/kill/退避/超时/资源回收                   │
│                帧编解码（v1 + v2 chunk）、请求-响应关联、事件归一化      │
│  store/        app.sqlite：schema 版本、迁移、索引、事件日志、整理账本   │
│  engine/       二进制定位/校验/版本探测/升级替换                       │
│  paths/        数据根、单实例锁、临时区、日志区                        │
└───────────────┬──────────────────────────────────────────────────────┘
                │ stdio NDJSON-RPC（stdin 命令 / stdout 事件）
┌───────────────▼──────────────────────────────────────────────────────┐
│ omp sidecar：`omp --mode rpc --cwd <workspace>`                      │
│  - 自持会话（jsonl）、配置（config.yml）、认证（agent.db）、blobs      │
│  - 经 PI_CODING_AGENT_DIR 全部落在我们的数据根内                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.1 所有权矩阵（谁写什么，不可越界）

| 数据 | 所有者 | 读 | 写 | 迁移者 |
| --- | --- | --- | --- | --- |
| `app.sqlite` | 我们（Rust） | Rust / 投影到 Vue | Rust | 我们的迁移链 |
| `omp-agent/**`（config.yml、agent.db、sessions、blobs、history.db） | **omp** | Rust 只读（索引、导出） | **仅 omp 进程** | omp 自身（会话 v1→v2→v3；settings.json→config.yml） |
| `runtime/<omp-version>/` | 我们（构建产物） | Rust | 安装/升级时整体替换 | 无（无用户数据） |
| `%TEMP%/omp-desktop/**` | 我们 | Rust | Rust | 不迁移（可重建） |
| `~/.omp/install-id` | omp | — | omp | 不动（见 §9 R4） |

不越界的具体约束（写进 review checklist）：

1. **禁止**在 Rust 里 `File::create` / 追加写 `sessions/**/*.jsonl`、`config.yml`、`agent.db`、`history.db`、`blobs/**`。
2. 需要改会话（重命名、压缩、分支、导出）时只能走 RPC：`set_session_name`、`compact`、`branch`、`export_html`、`handoff`。
3. 索引与搜索数据只存在 `app.sqlite`，是从 `omp-agent` 派生的可重建物。

---

## 4. 进程与 RPC 接入设计

### 4.1 打包：把 omp 二进制当 sidecar

- 构建期从 GitHub Release 拉取与目标三元组匹配的预编译二进制（见 §2.4 清单），校验并记录 sha256 到版本清单 `src-tauri/runtime/omp-runtime.json`：
  ```json
  {
    "version": "18.2.6",
    "targets": {
      "x86_64-pc-windows-msvc": { "asset": "omp-windows-x64.exe", "sha256": "…", "bytes": 0 },
      "aarch64-apple-darwin": { "asset": "omp-darwin-arm64", "sha256": "…", "bytes": 0 }
    }
  }
  ```
- `tauri.conf.json`：`bundle.externalBin: ["binaries/omp"]`（每个目标放 `binaries/omp-<target>[.exe]`），二进制随安装包分发，不依赖用户已装 Bun 或已装 omp。
- 开发/高级路径：环境变量 `OMP_DESKTOP_ENGINE_BIN` 指到本机 omp 时优先使用（便于对齐上游排查），但走同一套版本探测与校验分支（见 §4.3）。
- 不采用的方案：`bun install -g` 依赖用户环境（Bun ≥1.3.14 是硬门槛）；内嵌 npm 包（会额外引入 47MB JS 且运行仍需 Bun）。**推理由 §2.4 给出。**

### 4.2 启动参数与环境

```
argv: <engineBin> --mode rpc --cwd <workspaceRoot> [--approval-mode <always-ask|write|yolo>]
      [--add-dir <dir>]...            # 多根工作区时
cwd : <workspaceRoot>
env : 见下（显式构造，不继承父进程全部环境）
```

环境变量（`Command::env_clear()` 后逐条设置，白名单）：

| 变量 | 值 | 说明 |
| --- | --- | --- |
| `PI_CODING_AGENT_DIR` | `<dataRoot>/omp-agent` | **核心**：把 config.yml / agent.db / sessions / blobs / history.db 全收进应用数据根 |
| `PI_CODING_AGENT_SESSION_DIR` | 不设置 | 保持 omp 的「按 cwd 分桶」语义；若设置会退化到单一目录，丢失项目分组（§2.3） |
| `HOME` / `USERPROFILE` | 透传 | 供 omp 解析配置根（`install-id` 等）与用户级工具 |
| `PATH` | 透传 | agent 的 bash/git/lsp 需要 |
| `SystemRoot`、`TEMP`/`TMP`、`LANG`、`HTTPS_PROXY`/`HTTP_PROXY`/`NO_PROXY`、`NODE_EXTRA_CA_CERTS` | 透传 | Windows 必需 / 代理与自签 CA 场景 |
| `OMP_PROFILE` | **不设置** | 我们用默认 profile；命名 profile 会再套一层 `profiles/<name>/agent`，反而增加迁移面 |
| 其余（含 `ANTHROPIC_API_KEY` 等） | 不透传 | 凭据由应用内登录写入 `agent.db`；确需环境变量者由用户在设置里显式勾选白名单（见 §9 R3） |

注意：`PI_CONFIG_DIR` 只能给「home 下的目录名」而非绝对路径（§2.3），所以 `install-id` 与个别 root 级缓存仍留在 `~/.omp/`；这是明确的残留，不作为主存储路径依赖。

### 4.3 握手、协商与帧解码

1. spawn → 读 stdout 第一帧必须是 `{"type":"ready",...}`，超时（默认 15 s）判启动失败。
2. **必须实现 v2**：读 `supportedProtocolVersions`，包含 2 则发 `{"id":"protocol-1","type":"negotiate_protocol","protocolVersion":2}`，成功后才认为可用；否则降级 v1 并把「大响应可能被有界 fallback 截断」写入运行日志与健康状态。理由：会话导出、长历史 `get_messages`、大工具结果都会撞 1 MiB 物理帧上限（§2.1）。
3. v2 解码器校验规则（照协议实现，不自行放宽）：校验 `chunkId/index/count/byteLength`，拒绝交错或中断序列，超过 `maxReassembledFrameBytes` 显式失败，按 index 拼接后严格 UTF-8 解码再解析为单对象。
4. 记录协商结果到会话运行态：`{ engineVersion, protocolVersion, maxFrameBytes, maxReassembledFrameBytes }`。
5. 版本探测：启动后 `get_state`，并记录 `sessionFile`；引擎版本来自版本清单 + `--version` 探测（进程内一次性，不进 RPC 通道）。

### 4.4 请求/响应关联

- 所有出站命令带自生成 `id`（`<sessionId>:<seq>`），Rust 维护 `HashMap<id, oneshot>`；响应按 `id` 归还。**不得按到达顺序匹配**：`bash` 是并发派发的，多命令的响应顺序无保证（§2.1）。
- 必须处理协议边缘（§2.1）：`parse` 错误与未知命令的 response **无 `id`** —— 这类帧归入「会话级错误」通道，不能挂到某个 pending 请求上，也不能让 pending 永久悬挂（配超时 + 会话级 `agent_end`/退出事件兜底）。
- `prompt` ack 语义：ack 只表示「已接受」。**完成判定**：`agent_end` 且 `isTerminal !== false`；或 `data.agentInvoked:false`；或后续 `prompt_result`。`isTerminal:false` 只表示「还会继续」，不能当结束。（旧运行时可能没有该字段 → 缺省视为 terminal。）Rust 侧统一成 `TurnOutcome` 再投影给 Vue，避免前端理解协议细节。
- 历史读取走 `get_messages_page`：按 `nextCursor` 前进，遇到 `session_busy`（流式/压缩中）或 `stale_cursor`（快照变化）丢弃已取分页并回到 `get_messages` 的尽力快照，或直接重试整轮；错误分支按 `code` 判定，不匹配错误文案。
- 超时策略：连接类命令 15 s（get_state 等）、`prompt` 不设硬超时（由用户 abort 或 `max-time` 控制），但配「无事件心跳」看门狗（默认 120 s 无任何帧 → 标记 session 疑似停滞并提示）。

### 4.5 会话生命周期与并发

- 粒度：**一个 workspace × 一个常驻 sidecar 进程**，进程内一次一个活跃会话（`switch_session` 切换）。
  - 理由：会话文件按 cwd 分桶（§2.3），`--cwd` 是进程级；`new_session`/`switch_session` 不携带 cwd。
- 进程池：`maxProcesses`（默认 2，设置可调），空闲 `idleReapMs`（默认 5 min，且无进行中 turn）回收；回收前 `flush` 掉入站队列。
- 崩溃策略：子进程退出 → 该 workspace 所有会话标记 `runtime_state=stopped`；进行中的 turn 落库为 `interrupted`（不自动重发，符合 rules.md「禁止隐式重试」）；用户点继续时重新 spawn 并 `switch_session` 回到同一 jsonl。
- 关闭：向 stdin 关闭（协议规定的优雅退出路径）→ 等待退出（默认 10 s）→ 超时 kill 进程树；**进程退出前必须继续读 stdout**（§2.1）。
- 高频事件（`message_update` 等 delta）在 Rust 侧合并节流（默认 50 ms / 每帧上限 64 KiB）后再投递 Tauri 事件，落盘同理（批量写 `session_events`），满足 rules.md「高频交互不得逐次穿过 IPC」。

### 4.6 IPC 契约（Rust ↔ Vue）

Command（薄层，仅校验 + 调 service；命名稳定、DTO 显式序列化名）：

| command | 入参 → 出参 |
| --- | --- |
| `runtime_status` | `{}` → `{ engineVersion, protocolVersion, processes: RuntimeProcess[] }` |
| `session_list` | `{ workspaceRoot }` → `SessionSummary[]` |
| `session_open` | `{ workspaceRoot, sessionFile? }` → `SessionView`（spawn + switch_session 或 new_session） |
| `session_close` | `{ sessionId }` → `void` |
| `session_prompt` | `{ sessionId, text, attachments?, streamingBehavior? }` → `TurnAccepted` |
| `session_abort` / `session_steer` / `session_follow_up` | `{ sessionId, text? }` |
| `session_set_model` / `session_set_thinking` / `session_compact` / `session_branch` / `session_rename` / `session_export_html` | 会话操作直通 RPC |
| `session_messages` | `{ sessionId, cursor?, limit? }` → `{ messages, totalMessages, nextCursor?, source: 'page' \| 'snapshot' }`（底层 `get_messages_page`，按 §4.4 处理 `session_busy`/`stale_cursor`） |
| `session_delete` | `{ sessionId, deleteFiles: boolean }` → `void` |
| `settings_get` / `settings_update` | 应用设置（落 `app.sqlite`，替代 localStorage） |
| `maintenance_status` / `maintenance_run` | 迁移与整理账本、手动触发整理（`omp gc` 等） |
| `engine_versions` | 已装/可用引擎版本与校验状态 |

Event（`tauri::Emitter`，channel 名 `omp://session/<sessionId>`）：`turn_started`、`delta`（合并后）、`tool_call`、`tool_result`、`turn_finished`、`session_error`、`permission_request`、`runtime_exited`、`commands_updated`。
另有全局 `omp://runtime`：`engine_ready`、`engine_exit`、`maintenance_progress`。

约定：错误统一 `{ code, message }`（扩展现有 `IpcError`，code 稳定、message 面向用户）；不向前端回传堆栈、绝对路径或密钥。

---

## 5. 本地数据设计

### 5.1 数据根布局

数据根由 Tauri 路径 API 解析（`app_data_dir()/…`），Windows 下约 `%APPDATA%\com.omp.desktop\`：

```
<dataRoot>/
  app.sqlite            # 我们的库（唯一权威）
  app.sqlite-wal
  backups/              # 迁移前快照：app-v<from>-to-v<to>-<utc>.sqlite
  engine/               # 引擎二进制（无用户数据）
    <omp-version>/omp[.exe]
    current.json        # { version, sha256, installedAt }
  omp-agent/            # ← PI_CODING_AGENT_DIR（omp 独占）
    config.yml
    agent.db            # 认证库
    sessions/<encoded-cwd>/*.jsonl
    blobs/<sha256>
    history.db
    secrets.yml, secret-placeholder.key, terminal-sessions/…
  logs/
    app-<date>.log
    engine-<sessionId>-<date>.log
  scratch/              # 临时区（可随时清空），附件粘贴落这里
  .lock                 # 单实例锁
```

落地改动：附件从 `%TEMP%/omp-desktop/scratch`（`services/attachments.rs:47`）改到 `<dataRoot>/scratch/<sessionId>/`，由 paths 服务统一提供；临时区任何内容不得作为「数据」参与迁移。

### 5.2 `app.sqlite` schema v1

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
PRAGMA user_version = 1;              -- 迁移版本的唯一权威

-- 迁移/维护账本
CREATE TABLE schema_migrations (
  version      INTEGER PRIMARY KEY,
  app_version  TEXT    NOT NULL,
  applied_at   INTEGER NOT NULL,
  duration_ms  INTEGER NOT NULL
);
CREATE TABLE migration_runs (          -- 每次启动的迁移尝试（含失败）
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  from_version INTEGER NOT NULL,
  to_version   INTEGER NOT NULL,
  app_version  TEXT    NOT NULL,
  status       TEXT    NOT NULL CHECK (status IN ('running','done','failed','rolled_back')),
  backup_path  TEXT,
  error        TEXT,
  started_at   INTEGER NOT NULL,
  finished_at  INTEGER
);
CREATE TABLE maintenance_state (       -- 可中断的「整理」游标（§6.3）
  task       TEXT PRIMARY KEY,         -- 'reindex_sessions' | 'vacuum' | 'prune_scratch' | 'engine_gc' …
  status     TEXT NOT NULL CHECK (status IN ('idle','running','done','failed')),
  cursor     TEXT,                     -- 断点（如最后处理的 session 路径）
  stats_json TEXT,
  updated_at INTEGER NOT NULL
);

-- 项目
CREATE TABLE projects (
  id             INTEGER PRIMARY KEY AUTOINCREMENT,
  root_path      TEXT NOT NULL UNIQUE,   -- 规范化后的绝对路径
  name           TEXT NOT NULL,
  created_at     INTEGER NOT NULL,
  last_opened_at INTEGER NOT NULL,
  pinned         INTEGER NOT NULL DEFAULT 0
);

-- 会话索引（派生自 omp session 文件；可整表重建）
CREATE TABLE sessions (
  id             TEXT PRIMARY KEY,       -- omp session id（header.id）
  project_id     INTEGER REFERENCES projects(id) ON DELETE SET NULL,
  session_file   TEXT NOT NULL UNIQUE,   -- 绝对路径，指向 omp 的 jsonl
  cwd            TEXT NOT NULL,
  title          TEXT,                   -- 缓存；权威仍是文件 title 槽
  session_version INTEGER,               -- header.version（观察值）
  engine_version TEXT,                   -- 写入时的引擎版本
  cwd_bucket     TEXT,                   -- 该文件所在 encoded-cwd 目录名
  file_bytes     INTEGER,
  file_mtime_ms  INTEGER,
  indexed_at     INTEGER,
  created_at     INTEGER NOT NULL,
  updated_at     INTEGER NOT NULL,
  pinned         INTEGER NOT NULL DEFAULT 0,
  archived_at    INTEGER,
  deleted_at     INTEGER                 -- 软删：仅我们列表，不动文件
);
CREATE INDEX idx_sessions_project ON sessions(project_id, updated_at DESC);
CREATE INDEX idx_sessions_recent  ON sessions(updated_at DESC) WHERE deleted_at IS NULL;

-- 每个 turn 的账（跨重启可对账）
CREATE TABLE turns (
  id             TEXT PRIMARY KEY,        -- 我们的 turn id
  session_id     TEXT NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  runtime_state  TEXT NOT NULL CHECK (runtime_state IN ('running','done','failed','aborted','interrupted')),
  model_id       TEXT,
  thinking_level TEXT,
  input_tokens   INTEGER NOT NULL DEFAULT 0,
  output_tokens  INTEGER NOT NULL DEFAULT 0,
  started_at     INTEGER NOT NULL,
  ended_at       INTEGER,
  error_code     TEXT
);
CREATE UNIQUE INDEX idx_turns_one_running ON turns(session_id) WHERE runtime_state = 'running';

-- 事件日志（供 UI 重建，不替代 jsonl）
CREATE TABLE session_events (
  session_id TEXT    NOT NULL REFERENCES sessions(id) ON DELETE CASCADE,
  seq        INTEGER NOT NULL,
  turn_id    TEXT,
  kind       TEXT    NOT NULL,            -- 'delta'|'tool_call'|'tool_result'|'assistant_message'|'error'|…
  payload    TEXT    NOT NULL,            -- JSON（我们归一化后的形状）
  at         INTEGER NOT NULL,
  PRIMARY KEY (session_id, seq)
) WITHOUT ROWID;

-- 应用设置（版本化，替代 localStorage）
CREATE TABLE settings (
  key        TEXT PRIMARY KEY,
  value_json TEXT NOT NULL,
  updated_at INTEGER NOT NULL
);
CREATE TABLE settings_migrations (     -- 从 localStorage 一次性导入的幂等标记
  key        TEXT PRIMARY KEY,
  applied_at INTEGER NOT NULL
);

-- 附件（粘贴/导入的持久副本或引用）
CREATE TABLE attachments (
  id         TEXT PRIMARY KEY,
  session_id TEXT,
  kind       TEXT NOT NULL CHECK (kind IN ('image','text','file')),
  name       TEXT NOT NULL,
  mime_type  TEXT NOT NULL,
  bytes      INTEGER NOT NULL,
  path       TEXT NOT NULL,              -- 绝对路径（scratch 或复制后的持久路径）
  sha256     TEXT,
  created_at INTEGER NOT NULL
);
CREATE INDEX idx_attachments_session ON attachments(session_id, created_at);
```

设计要点：

- **`sessions.session_file` 是索引，不是所有权**。文件被用户在外部（CLI）删除时，整理流程标 `deleted_at` 或移除行，绝不重建文件。
- `session_events.payload` 存归一化后的 JSON：我们的 UI 形状与 omp 的 wire 形状解耦，引擎升级不会污染历史事件。
- 不存任何密钥；`agent.db` 的凭据永不进 `app.sqlite`。

### 5.3 读路径与写路径

- 写：会话操作 → RPC → 事件回来 → 归一化 → 在**同一事务**内写 `session_events` + 更新 `turns`/`sessions.updated_at`。`seq` 由 Rust 单调分配，保证重放顺序。
- 读：UI 列表读 `sessions`/`turns`（不碰 jsonl）；历史消息以 RPC `get_messages_page` 为权威（omp 自己的重建逻辑正确），我们的 `session_events` 只承担两件事——(a) 流式期间的增量重放，(b) 离线搜索/预览索引。二者冲突时以 RPC 分页结果为准并回写索引。
- 外部变更（用户在终端用 omp CLI 改过会话）：`session_list` 时按 `file_mtime_ms`/`file_bytes` 判定脏，重建该行索引与搜索投影（仍只读 jsonl，不写回）。

---

## 6. 版本迭代与迁移机制

### 6.1 两个版本号，互不耦合

| 版本 | 权威位置 | 谁维护 | 失败后果 |
| --- | --- | --- | --- |
| 应用数据 schema | `app.sqlite` 的 `PRAGMA user_version` | 我们 | 由我们原子回滚 + 备份恢复 |
| 引擎/会话格式 | `omp-agent/sessions/*.jsonl` header `version`（当前 3）+ `runtime/current.json` 记录的引擎版本 | omp | 由 omp 加载时自行迁移；我们只观察、不介入 |

我们在 `runtime/current.json` 与 `settings` 中记录「上次成功启动的引擎版本」，用于升级后的一次性兼容检查（§6.4）。

### 6.2 迁移执行流程（启动期，硬门闩）

```
启动
 → 获取单实例锁（<dataRoot>/.lock）；拿不到 → 聚焦已有窗口并退出
 → 打开 app.sqlite
     ├ user_version <  SCHEMA_VERSION → 进入迁移流程
     ├ user_version == SCHEMA_VERSION → 直接进入服务
     └ user_version >  SCHEMA_VERSION → FAIL-CLOSED：只读模式启动
          标题：「数据来自更新版本的应用」+ 提示升级应用 / 打开数据目录；不出会话功能
 → 迁移流程
     1) 写 migration_runs(status='running')
     2) VACUUM INTO backups/app-v<from>-to-v<to>-<utc>.sqlite
     3) 逐个版本：BEGIN IMMEDIATE → DDL/DML → PRAGMA user_version=n → COMMIT
        （单版本单事务；失败 → ROLLBACK，回滚该版本，status='rolled_back'）
     4) 成功：写 schema_migrations、status='done'，保留最近 3 个备份，其余按名字排序删除
     5) 失败：status='failed' + error；保留备份；弹出阻断式提示（重试 / 用备份恢复 / 打开数据目录）
 → 迁移成功后（异步、不阻塞 UI）执行「整理」pass
 → 引擎侧兼容检查（§6.4）
 → 启动 RPC 服务
```

迁移代码纪律（写进 PR 模板 / review）：

1. 迁移只做加法与回填：`ADD COLUMN`、`CREATE TABLE/INDEX`、`UPDATE … SET`。**禁止**在迁移中 `DROP COLUMN/TABLE`、改语义、删数据；需要淘汰时先停止读写 → 下一个大版本再物理删除（两版法）。
2. 每个迁移带 `applied_at` 与所属 `app_version`，可追溯「哪一版引入了该变更」。
3. 迁移函数必须幂等（`IF NOT EXISTS`、`WHERE` 条件收敛），因为重试是允许的操作。
4. 迁移测试用真实旧库快照（fixtures：v1 库样本）跑，而不是空库。
5. `SCHEMA_VERSION` 常量与迁移数组长度一致，由单元测试断言（防止「加了迁移忘了改常量」）。

### 6.3 「整理」（maintenance）——下一个版本对上一个版本数据的归一化

与结构迁移分开，逐任务可中断、可重入，进度写 `maintenance_state.cursor`。v1 任务清单：

| 任务 | 做什么 | 幂等/中断策略 |
| --- | --- | --- |
| `import_local_settings` | 把 `localStorage['omp-desktop.settings']` 一次性导入 `settings` 表 | `settings_migrations` 标记；导入成功后 **保留** localStorage 一个版本周期再删 |
| `migrate_scratch_paths` | 把 `%TEMP%/omp-desktop/scratch/**` 里仍被 `attachments` 引用的文件搬到 `<dataRoot>/scratch/`，改写 `attachments.path` | 逐条事务；已存在目标则跳过 |
| `reindex_sessions` | 扫描 `omp-agent/sessions/**`，按 mtime/size 增量建立/刷新 `sessions` 行 | cursor = 最后处理的文件路径；可重入 |
| `prune_orphans` | 删除 `attachments` 中指向不存在文件的记录；删除 `session_events` 中 session 已被移除的孤儿行 | 先计数再删；单事务 |
| `compact_events` | 合并相邻 `delta` 事件为聚合块，超期事件（默认 30 天且 turn 已终结）降采样 | 按 session 分批；cursor 记录进度 |
| `vacuum` | `VACUUM`（WAL checkpoint 后）回收空间 | 失败不影响功能；下次启动重试 |
| `engine_gc` | 调用 `omp gc --apply`（把 `PI_CODING_AGENT_DIR` 指到我们目录内，语义与 CLI 一致）清扫无引用 blob、归档冷会话 | 只在「距上次成功 > 7 天」或用户手动触发时跑；命令输出写 `maintenance_state.stats_json` |
| `prune_scratch` | 清空 `scratch` 中未被 `attachments` 引用的文件 | 可随时执行 |

**不迁移、只重建**：任何缓存、缩略图、语法高亮索引、搜索结果缓存 —— 直接删，避免把迁移面做大。

### 6.4 升级路径（发新版给别人安装时会发生什么）

应用更新（Tauri updater / 手动安装）后首次启动：

1. 单实例锁生效，旧进程已退出（安装器语义保证）。
2. `app.sqlite` 走 §6.2 迁移链；schema 变化是**唯一**需要结构性处理的东西。
3. 引擎替换：`engine/<new-version>/` 就位后更新 `engine/current.json`；旧版本目录保留一个周期（回滚用），下次启动清旧的。
4. 引擎兼容检查（只读，不写 omp 数据）：
   - 读 `runtime/current.json` 的旧引擎版本 vs 新引擎版本；
   - 抽样 2–3 个会话文件头（只读前 8 KiB）解析 header 的 `version`；若 `> 3`（未来格式）→ 说明用户曾用更高版本 omp 写过：**标记这些会话为「需更新引擎」**，不索引、不覆盖，UI 灰显并给说明；
   - 若引擎版本升级 → 触发一次 `reindex_sessions`（会话格式可能变化）；
   - 记录 `settings['engine.lastCheckedVersion']`。
5. 回滚路径：用户装回旧版应用 → 旧版看到 `user_version > SCHEMA_VERSION` → 只读 + 明确提示；因为引擎会话文件从未被我们改写，用户用 omp CLI 仍可读到全部会话。

关键不变量（每次 review 复核）：

- I1 应用升级只可能改 `app.sqlite` 与 `engine/`，**永不**改 `omp-agent/` 里的会话与配置。
- I2 任何 schema 变更前必有可用备份，且备份不自动全删（留 3 个）。
- I3 未知的更高版本一律 fail-closed，不降级、不清洗、不猜。
- I4 索引可整表重建：删掉 `sessions`/`session_events` 后能仅凭 `omp-agent` 恢复功能。

### 6.5 每次发版的搬运清单

1. 引擎版本是否要跟着升？升则更新 `omp-runtime.json`（version + 各目标 sha256）并跑一次「引擎兼容检查」用例。
2. 有 schema 变更则 `SCHEMA_VERSION + 1` 且附迁移 + 用旧库 fixture 跑测试。
3. 需要「整理」旧数据则加 maintenance 任务（幂等 + cursor），不要写进迁移。
4. 更新 `capabilities`（仅当有新权限）、前端 DTO 与 `desktopApi/agentApi` 调用方（rules.md §6）。
5. 手动验证：旧库升级（含失败注入）、引擎替换、会话续聊、导出。

---

## 7. 模块落位（遵守 `rules.md` 1000 行上限与分层）

Rust（`src-tauri/src/`）：

```
lib.rs                 仅初始化、插件、command 注册、state 装配（保持 <120 行）
state.rs               新增：AppState { paths, db, runtime, engine }
errors.rs              扩展：稳定 code 常量（ENGINE_*, DB_*, MIGRATION_*）
models/                dto：runtime.rs / session.rs / event.rs / settings.rs / maintenance.rs
commands/
  runtime.rs           runtime_status、engine_versions
  sessions.rs          session_*（薄：校验 + 调 service）
  settings.rs          settings_get/update
  maintenance.rs       maintenance_status/run、备份列举与恢复入口
  （既有 attachments.rs / git.rs / workspace.rs / models.rs 保持不变，仅附件根路径改走 paths）
services/
  paths.rs             数据根解析、各子目录、单实例锁、临时区
  engine.rs            二进制定位/清单校验(sha256)/版本探测/替换
  runtime/
    mod.rs             对外类型与重导出
    supervisor.rs      进程池：spawn/退出/退避/回收/优雅关闭
    protocol.rs        NDJSON 编解码 + v2 chunk 解码器（独立单测）
    client.rs          id 关联、待决表、超时、错误映射
    events.rs          事件归一化（wire → 我们的形状）+ 合并节流
    session.rs         会话生命周期（open/switch/close/turn 状态机）
  store/
    mod.rs             连接与线程策略（写连接 Mutex + spawn_blocking；只读连接供 UI 查询）
    schema.rs          SCHEMA_VERSION + v1 DDL
    migrations.rs      迁移链 + 备份/恢复 + user_version 门闩
    index.rs           会话索引（只读扫描 jsonl 头/尾、增量重建）
    events.rs          session_events / turns 写入与分页读
    settings.rs        设置读写 + localStorage 导入
    maintenance.rs     §6.3 任务实现 + cursor
```

前端（`src/`）：

```
utils/agentApi.ts        新增：RPC 相关 command 的集中封装 + 类型化事件订阅
utils/agentTypes.ts      新增：DTO 类型（与 Rust 显式序列化名同源）
utils/desktopApi.ts      保留（工作区/Git/模型），不混入会话逻辑
stores/sessionStore.ts   新增：会话列表/活跃会话/turn 状态（替换 useAppShell 里的内存 mock）
stores/appSettings.ts    改：读写走 settings_get/update（localStorage 仅作一次性导入源）
composables/useAgentSession.ts  新增：流式订阅、合并 delta 应用、abort
components/SourceConversation* 保留结构，数据源换成 store
```

新增依赖（每个都需在评审中给出理由，符合 rules.md §7）：

| 依赖 | 用途 | 理由 |
| --- | --- | --- |
| `rusqlite`（`bundled`） | 本地库 | 与 PI-Desktop `host-core` 同选型（`Cargo.lock` 已见 rusqlite 0.40 + bundled，验证过长尾生态）；bundled 免装系统 SQLite，跨平台可复现 |
| `sha2` | 引擎二进制/附件校验 | 需要校验发行产物哈希；无现成依赖可用 |
| `tauri-plugin-single-instance`（可选） | 单实例 + 聚焦已有窗口 | 不引入会让「双开写同一个 `agent.db`/`app.sqlite`」成为必然事故；若评审否决，降级为纯文件锁（无聚焦体验） |

明确不加：任何 Agent SDK、任何 JSON-RPC 库（协议窄且需自定义 v2 分片与未知命令容错，通用库反而添乱）、任何 ORM。

---

## 8. 分阶段实施与验收

| 阶段 | 内容 | 验收（可观察） |
| --- | --- | --- |
| P0 骨架 | paths/单实例锁/engine 校验/日志；supervisor + protocol(v1+v2) + client；`runtime_status` | `cargo check` 通过；应用启动后 spawn 成功、ready→negotiate 2、`get_state` 返回；杀掉子进程后自动标记 `runtime_exited` |
| P1 会话可用 | `session_list/open/prompt/abort` + 事件归一化 + Vue 流式渲染 | 真实工作区发一次 prompt，看到流式文本、工具卡与结束态；abort 立即停；重启应用后能列出并续聊同一会话 |
| P2 持久化与迁移 | `app.sqlite` schema v1 + 迁移链 + 备份/恢复 + 单实例门闩 + fail-closed | 用 v1 fixture 库跑迁移通过；手工把 `user_version` 置为 999 → 只读提示；删除 `sessions` 表数据后整理能重建 |
| P3 整理与引擎升级 | §6.3 全部 maintenance 任务 + §6.4 兼容检查 + `settings` 落库 + 附件路径迁移 | 旧版数据（localStorage 设置 + temp 附件）升级后可用；`omp gc --apply` 由应用触发且账本可查；引擎版本切换后会话仍可打开 |
| P4 扩展 | host tools / host URIs（`set_host_tools`/`set_host_uri_schemes`，注意 `security://` 为 omp 保留、宿主不可注册；`edit` 工具不作用于 host URI，需用 `write` 走整文件替换）、subagent 订阅（默认 `off`）、分支树 UI、FTS 搜索 | 按需立项，不在本次范围 |

---

## 9. 风险与未决问题

| # | 风险/未决 | 影响 | 处置 |
| --- | --- | --- | --- |
| R1 | 每进程常驻内存未知（官方为 187–208 MB 的 Bun 单文件产物，RSS 未实测） | 多 workspace 同开时内存放大 | **[INFERENCE]** 先按 `maxProcesses=2` 上线并实测；设置可调；空闲回收 |
| R2 | 首次使用无任何凭据 → 引擎报 `No model selected`；且 RPC 的 `login` **不接受** `secret: true` 的输入提示（一律拒绝，引导去终端 UI） | 首次体验；部分 provider 无法在应用内完成登录 | 引导走 `get_login_providers` + `login`：API-key 型 provider 由我们在应用内收集后写入凭据（写入路径见 R2a）；需要密文输入的 provider 明确提示「请在终端执行 `omp /login <provider>`」，并在完成后重新探测 | 
| R2a | 凭据写入路径未定：omp 的凭据存于 `agent.db`，我们无官方写入 API | 阻断登录闭环 | 方案 A（优先，待实测）：`login` 流程中对非 secret 提示正常应答，key 型 provider 走普通 `input`（非 `secret`）应答，由 omp 自己落库；方案 B（兜底）：用户在终端完成一次登录。**上线前必须实测 A 是否成立** |
| R3 | 不透传父进程环境会切断「用户已有 `ANTHROPIC_API_KEY` 等」的既有习惯 | 迁移体验 | 设置里提供显式环境变量白名单（默认空），并在引导页说明；默认安全优先 |
| R4 | `install-id` 与个别 root 级缓存仍在 `~/.omp/`（`PI_CONFIG_DIR` 只接受目录名） | 与用户自装 omp 共享安装身份；卸载不彻底 | 文档说明；不依赖该路径存用户数据。**[INFERENCE]** 若后续需要彻底隔离，可评估用 `HOME` 重定向或向上游提 `PI_CONFIG_ROOT` 绝对路径支持 |
| R5 | 引擎升级若引入会话格式 v4 | 老应用读不了新会话 | §6.4 兼容检查：`version > 3` 的会话标记为「需更新引擎」，只读灰显，绝不索引/改写 |
| R6 | RPC 协议 v3 出现 | 协商失败 | 已按「读 ready 协商、失败即降级并显式告警」设计，不硬编码单版本 |
| R7 | 未知命令 / parse 错误不带 `id`（§2.1） | 请求悬挂 | client 层把这些帧归入会话级错误通道；pending 有超时兜底 |
| R8 | macOS 代码签名/公证需覆盖 sidecar | 分发受阻 | 发布流程把 sidecar 一并签名（Tauri 打包会处理 externalBin；CI 需验证 notarization 通过）——待发布阶段验证 |
| R9 | `omp gc --apply` 在自定义 `PI_CODING_AGENT_DIR` 下的作用域 | 误删风险 | **[INFERENCE]** 期望其按 agent dir 解析；上线前用隔离目录实测「GC 只动我们目录内的 blob/会话」，通过后才在应用内自动触发（否则仅提供手动按钮） |
| R10 | 双实例写同一个 `app.sqlite`/`agent.db` | 数据损坏 | 单实例锁（§6.2 第 0 步）；omp 自身无跨进程写锁，必须由我们把关 |

---

## 10. 自检记录（第二遍核验）

### 10.1 与 `rules.md` 的逐条核对

| 规范 | 本方案的落点 |
| --- | --- |
| §1 前端 Vue3+TS；Tauri 2；系统能力用 Rust | §3/§7：Rust 独占进程、落盘、迁移；Vue 仅投影（§4.6 契约） |
| §1「当前阶段不接入 Agent SDK」 | 本方案接入的是 omp **CLI 的 RPC 协议**（子进程 + stdio），不是任何 SDK 依赖；且用户本次明确要求接入 |
| §2 开发前架构检查（需求/边界/数据流/错误场景/DTO 先行） | §1–§4 已给边界与所有权；§5.2 先定 DDL，§4.6 先定 command/event 契约 |
| §3.3 单文件 ≤1000 行 | §7 已按职责拆分（runtime/store/engine/paths 各自独立，单文件目标 300–600 行） |
| §4 前端：集中式 API 封装、无 `any`、异步要处理取消/过期 | §4.6 只暴露 typed command/event；§7 用 `utils/agentApi.ts` 统一封装；Rust 侧负责取消（abort）与过期响应丢弃（seq/幂等） |
| §5 Rust：command 薄、service 承载、外部输入校验、无 `unwrap`、有边界、不阻塞异步运行时、任务可取消、原子写 | §7 结构；§4.5 进程池与回收；§6.2 事务与备份；DB 走 `spawn_blocking`；DB 写用事务 + 原子替换式备份（`VACUUM INTO`） |
| §6 IPC：集中封装、DTO 同步、事件区分类型、错误稳定码、capabilities 最小权限 | §4.6；capabilities 保持 `core:default`+`dialog:default`（子进程由 Rust 直管，**不需要** shell 插件权限） |
| §7 依赖：先确认现有不足、锁版本、关多余 feature | §7 依赖表逐项理由；`rusqlite` 使用 `bundled`，其余 feature 不加 |
| §8 状态/并发/可靠性：状态所有者明确、无隐式重试、缓存有界 | §3.1 所有权矩阵；§4.5 崩溃不自动重发；§6.3 缓存一律重建不迁移 |
| §9 安全：不信任前端、路径穿越、密钥不落日志 | §4.2 环境白名单；§5.2 不存密钥；§4.6 不回传密钥/绝对路径；附件 sessionId 校验沿用现有 `is_safe_session_id` |
| §10 验证：类型检查/构建/行为验证、不虚报 | §8 每阶段验收 + §10.2 验证计划 |
| §11 变更/交付：只改必要文件、干净切换、交付清单 | §6.5 发版清单；§8 分阶段落地 |

### 10.2 契约一致性核对（逐条回溯 §2）

- 「prompt ack ≠ 结束」→ §4.4：完成判定为 `agent_end` 且 `isTerminal !== false`（`false` 只代表还会继续），另有 `data.agentInvoked:false` 与 `prompt_result` 两条本地完成路径。
- 「历史必须分页」→ §4.4/§4.6：`get_messages_page` + `nextCursor`，`session_busy`/`stale_cursor` 按 code 分支处理。
- 「并发命令响应乱序」→ §4.4：一律按 `id` 匹配，禁止按到达顺序。
- 「RPC login 拒绝 secret 输入」→ §9 R2/R2a：应用内不收集密文凭据，必要时回落终端登录。
- 「v1 1 MiB 有界 fallback」→ 强制实现 v2（§4.3），非可选。
- 「帧上限/重组上限来自 ready」→ §4.3 记录协商值，不硬编码。
- 「stdin 关闭后仍要读 stdout」→ §4.5 关闭流程显式约束。
- 「未知命令 response 无 id」→ §4.4 与 §9 R7 处理。
- 「会话按 cwd 分桶」→ §4.2 不设 `PI_CODING_AGENT_SESSION_DIR`；§4.5 进程粒度按 workspace。
- 「`PI_CODING_AGENT_DIR` 重定位 agent 全量」→ §4.2 是数据隔离的主开关（有 docs/settings.md 出处）。
- 「会话 v1→v3 由 omp 迁移」→ §3.1/§6.1 我们不改写 jsonl。
- 「blobs 引用 `blob:sha256:`；`omp gc` 管它」→ §6.3 `engine_gc` 交给 omp；R9 实测前提。
- 「`settings.json` 一次性迁移」→ 我们不依赖该迁移，只读 `config.yml` 语义（§3.1）。

### 10.3 失败模式推演

| 场景 | 期望行为 | 方案依据 |
| --- | --- | --- |
| 迁移中途断电/被杀 | 启动时发现 `migration_runs.status='running'` 且 `user_version` 未变 → 按未迁移处理，重跑；备份仍在 | §6.2 单版本单事务 + 账本 |
| 迁移后应用崩溃，用户装回旧版 | 旧版见 `user_version` 更高 → 只读提示，不清洗 | §6.2 fail-closed、I3 |
| 用户删掉 `app.sqlite` | 会话仍在 `omp-agent`；应用重建库并 reindex | I4 |
| 用户删掉 `omp-agent/sessions` | `session_list` 的索引指向不存在文件 → 整理标删，UI 消失，但不伪造数据 | §5.3、§6.3 `prune_orphans` |
| 引擎升级后会话打不开 | R5 检查：header 版本高于已知 → 标记「需更新引擎」而非报错崩溃 | §6.4 |
| 并发两个会话同时 prompt | 进程粒度按 workspace，进程内串行；不同 workspace 并行受 `maxProcesses` 限制 | §4.5 |
| RPC 帧 > 1 MiB 且未协商 v2 | 显式记录降级状态；导出/长历史接口提示可能截断 | §4.3 |

### 10.4 本轮未执行/未验证项（必须由实现阶段补）

1. 未运行任何构建、测试或实机验证（本轮交付物是方案文档，不含代码改动）。
2. 引擎二进制 sha256 与各目标资源在实际发布中存在性、以及 `externalBin` 的具体命名规则需在 P0 用真实构建验证。
3. R1 内存占用、R9 `omp gc --apply` 作用域、R2a 凭据写入闭环（`login` 对 key 型 provider 是否以非 `secret` 的普通 `input` 提示、应答后是否由 omp 自行落库）需实测。
4. `switch_session` 传入非当前 cwd 桶的 `sessionPath` 行为（跨项目切换）未验证 —— 方案按「进程按 workspace 隔离」规避，不依赖该路径。
5. Tauri v2 `externalBin` 在 Windows 下是否需要 `.exe` 后缀对齐，需按 P0 构建结果确认。
6. `get_messages_page` 在 v1（未协商 v2）下遇到单条超限消息会报 overflow —— 需实测真实会话是否必须强制 v2（当前方案已强制协商 v2，此项用于验证该强制的必要性）。

### 10.5 第二遍核验修正记录（本轮实际改动）

对官方 `docs/rpc.md`（can1357/oh-my-pi，main）逐段复核后，修正了初稿的 5 处偏差：

| # | 初稿 | 复核结论 | 落点 |
| --- | --- | --- | --- |
| 1 | 以 `agent_end` 作为轮次结束 | 必须以 `isTerminal !== false` 判定；`false` 表示还会继续 | §2.1、§4.4 |
| 2 | 历史消息只靠自有事件日志 | 官方提供 `get_messages_page`（cursor 语义、256/页、`session_busy`/`stale_cursor`），应作为权威读路径 | §2.1、§4.4、§4.6、§5.3 |
| 3 | 未提并发命令乱序 | `bash` 并发派发，跨命令响应顺序不保证，必须按 `id` 匹配 | §2.1、§4.4 |
| 4 | 假设应用内可完成任意 provider 登录 | `login` 拒绝 `secret: true` 输入提示；密文类 provider 必须回落终端 | §2.1、§9 R2/R2a |
| 5 | 启动参数写了 `--no-title` | RPC 模式本就默认关闭自动标题、抑制 `setTitle` 事件（`PI_RPC_EMIT_TITLE=1` 可开）→ 该旗标多余，删除 | §4.2 |

同时补齐：`negotiate_protocol` / `set_fast_mode` / `get_messages_page` 三条命令、扩展事件名（`retry_fallback_*`、`model_changed`、`thinking_level_changed`、`irc_message`、`notice`、`goal_updated`），并记录 `security://` 为 omp 保留 scheme（P4 设计约束：宿主不得注册或遮蔽）。

### 10.6 结论

- 与 `rules.md` 无冲突；所有对 omp 的行为假设均可回溯到 §2 的官方文档/发布产物，少数推断已标 **[INFERENCE]**。
- 升级安全性的核心不变量 I1–I4 已由「数据分三层 + 所有权互斥 + 迁移与整理分离 + fail-closed」共同保证。
- 待评审确认项：D1（sidecar 打包体积换运行形态）、§4.2 环境白名单（R3）、P4 范围（是否本次纳入 host tools）。
